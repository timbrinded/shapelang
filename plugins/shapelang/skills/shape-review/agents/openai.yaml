interface:
  display_name: "Shape Review"
  short_description: "Review a code change for real bugs"
  default_prompt: "Use $shape-review to review this change for concrete local and cross-object bugs, treating authored Shape as evidence and verifying every Shape-derived lead against reachable source behavior."
