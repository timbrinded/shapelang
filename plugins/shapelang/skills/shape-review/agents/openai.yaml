interface:
  display_name: "Shape Review"
  short_description: "Review a code change for real bugs"
  default_prompt: "Use $shape-review to review this change for real bugs, using the Shape model to surface cross-object issues the diff alone can't show."
