interface:
  display_name: "Shape Review"
  short_description: "Review a code change for real bugs"
  default_prompt: "Use $shape-review to find concrete reachable bugs in this code diff. Verify every Shape-derived lead in source, challenge plausible prevention mechanisms, and keep stale-model warnings separate from code comments."
