interface:
  display_name: "Shape Index"
  short_description: "Build a whole-codebase Shape model"
  default_prompt: "Use $shape-index from a fixed clean baseline. Keep generated AST read-only, author high-confidence subsystems in current-CLI batches, preserve stable evidence and important typed review context, and report explicit gaps."
policy:
  allow_implicit_invocation: false
