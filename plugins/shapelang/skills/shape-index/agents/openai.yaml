interface:
  display_name: "Shape Index"
  short_description: "Build a whole-codebase Shape model"
  default_prompt: "Use $shape-index from a fixed clean baseline. Inventory every architecture-significant subsystem first, then author only source-supported contracts in validated batches and report explicit gaps."
policy:
  allow_implicit_invocation: false
