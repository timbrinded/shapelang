interface:
  display_name: "Shape Contract Guard"
  short_description: "Review Shape contract-diff risk"
  default_prompt: "Use $shape-contract-guard to normalize this authored Shape diff into exact before/after facts, classify impact separately from support, and return only verified contract-risk findings."
