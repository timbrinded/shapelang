interface:
  display_name: "Shape Contract Preflight"
  short_description: "Plan code changes against Shape"
  default_prompt: "Use $shape-contract-preflight before editing. Separate known facts, assumptions, and unknowns; inspect focused current-model constraints; and return one explicit planning decision with any baseline or proposal diagnostics."
