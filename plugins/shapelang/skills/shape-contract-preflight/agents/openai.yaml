interface:
  display_name: "Shape Contract Preflight"
  short_description: "Plan code changes against Shape"
  default_prompt: "Use $shape-contract-preflight before editing. Separate known facts, assumptions, and unknowns; represent the complete end-to-end architecture outcome with stable source/evidence anchors; and return one explicit planning decision with baseline or proposal diagnostics."
