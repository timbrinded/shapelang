interface:
  display_name: "Shape Contract Preflight"
  short_description: "Plan code changes against Shape"
  default_prompt: "Use $shape-contract-preflight to plan this change against the current authored Shape model before editing code, including strict guard, path, domain-pack, coverage, and binding constraints."
