[
  {
    "id": "lang-draft-strict",
    "skill": "shape-lang",
    "task": "Determine whether the unknown-effect model is acceptable as a draft and as a final model.",
    "fixture": "fixtures/fail/unknown_effects/audit.shape",
    "required_commands": [
      "bun shp check --allow-unknown-effects fixtures/fail/unknown_effects/audit.shape",
      "bun shp check fixtures/fail/unknown_effects/audit.shape"
    ]
  },
  {
    "id": "lang-final-forbid",
    "skill": "shape-lang",
    "task": "Determine whether Memory Guard context can waive the final forbid.",
    "fixture": "fixtures/fail/memory_guard_does_not_override_final_forbid/audit.shape",
    "required_commands": [
      "bun shp check fixtures/fail/memory_guard_does_not_override_final_forbid/audit.shape"
    ]
  },
  {
    "id": "preflight-guarded-unknown",
    "skill": "shape-contract-preflight",
    "task": "Simulate the guarded refactor with incomplete effect knowledge and distinguish the unknown-effect warning from the guard failure.",
    "fixture": "fixtures/skills/preflight/guarded-unknown",
    "required_commands": [
      "plugins/shapelang/skills/shape-contract-preflight/scripts/precheck.sh --shape-root fixtures/skills/preflight/guarded-unknown/shape --json fixtures/skills/preflight/guarded-unknown/proposal.shape"
    ]
  },
  {
    "id": "preflight-invalid-baseline",
    "skill": "shape-contract-preflight",
    "task": "Identify whether the baseline or proposal is responsible for the failed simulation.",
    "fixture": "fixtures/skills/preflight/invalid-baseline",
    "required_commands": [
      "plugins/shapelang/skills/shape-contract-preflight/scripts/precheck.sh --shape-root fixtures/skills/preflight/invalid-baseline/shape --json fixtures/skills/preflight/invalid-baseline/proposal.shape"
    ]
  },
  {
    "id": "preflight-complete-route",
    "skill": "shape-contract-preflight",
    "task": "Before implementing archive publishing, preflight the supplied proposal for an outcome in which SubmissionApi retrieves PublishedArchive through ArchiveWorker.",
    "fixture": "fixtures/skills/preflight/complete-route",
    "required_commands": [
      "plugins/shapelang/skills/shape-contract-preflight/scripts/precheck.sh --shape-root fixtures/skills/preflight/complete-route/shape --json fixtures/skills/preflight/complete-route/proposal.shape"
    ]
  },
  {
    "id": "guard-policy-removal",
    "skill": "shape-contract-guard",
    "task": "Classify the authored contract change without reading application source.",
    "fixture": "fixtures/skills/guard/policy-removal",
    "required_commands": [
      "bun shp check fixtures/skills/guard/policy-removal/candidate/contract.shape"
    ]
  },
  {
    "id": "guard-equivalent-relocation",
    "skill": "shape-contract-guard",
    "task": "Determine whether deleting the base file removed the contract or relocated it unchanged.",
    "fixture": "fixtures/skills/guard/equivalent-relocation",
    "required_commands": [
      "bun shp check fixtures/skills/guard/equivalent-relocation/candidate/retention.shape"
    ]
  },
  {
    "id": "index-missing-ast",
    "skill": "shape-index",
    "task": "Decide how whole-repository indexing should proceed when generated AST navigation is absent.",
    "fixture": "fixtures/skills/index/missing-ast",
    "required_commands": [
      "bun shp check fixtures/skills/index/missing-ast/shape/system.shape"
    ]
  },
  {
    "id": "index-no-invariant",
    "skill": "shape-index",
    "task": "Classify the formatting utility without manufacturing an architecture invariant.",
    "fixture": "fixtures/skills/index/no-invariant",
    "required_commands": []
  },
  {
    "id": "index-coverage-gaps",
    "skill": "shape-index",
    "task": "Refresh the miniature image-processing repository's authored model and report whether it is complete, including all source- and documentation-supported gaps.",
    "fixture": "fixtures/skills/index/coverage-gaps",
    "required_commands": [
      "bun shp check fixtures/skills/index/coverage-gaps/shape/system.shape"
    ]
  },
  {
    "id": "review-cross-object",
    "skill": "shape-review",
    "task": "Review the candidate change for a reachable bug and use Shape only to locate the affected caller.",
    "fixture": "fixtures/skills/review/cross-object",
    "required_commands": [
      "bun shp check fixtures/skills/review/cross-object/shape/model.shape"
    ]
  },
  {
    "id": "review-stale-model",
    "skill": "shape-review",
    "task": "Determine whether the contradicted authored relation is a code bug comment or a separate model warning.",
    "fixture": "fixtures/skills/review/stale-model",
    "required_commands": [
      "bun shp check fixtures/skills/review/stale-model/shape/model.shape"
    ]
  },
  {
    "id": "review-root-cause-grouping",
    "skill": "shape-review",
    "task": "Review the candidate range change for real reachable bugs, using the fixture's contract and authored Shape model as evidence.",
    "fixture": "fixtures/skills/review/root-cause-grouping",
    "required_commands": [
      "bun shp check fixtures/skills/review/root-cause-grouping/shape/model.shape",
      "bun shp explain RangeNormalizer.normalizeRange fixtures/skills/review/root-cause-grouping/shape/model.shape",
      "bun shp graph show RangeNormalizer fixtures/skills/review/root-cause-grouping/shape/model.shape"
    ]
  },
  {
    "id": "visualiser-deterministic-nested-model",
    "skill": "unix-system-visualiser",
    "task": "Generate the visualiser twice from the nested authored model, prove that both HTML files are identical, and report the modeled declaration counts without treating them as runtime proof.",
    "fixture": "fixtures/skills/unix-system-visualiser/connected",
    "required_commands": [
      "bun shp check fixtures/skills/unix-system-visualiser/connected/shape/nested/system.shape",
      "bun plugins/shapelang/skills/unix-system-visualiser/scripts/generate.mjs --repo fixtures/skills/unix-system-visualiser/connected --output .research/atlas-a.html --shape-command \"bun ../../../../packages/shp-cli/src/index.ts\"",
      "bun plugins/shapelang/skills/unix-system-visualiser/scripts/generate.mjs --repo fixtures/skills/unix-system-visualiser/connected --output .research/atlas-b.html --shape-command \"bun ../../../../packages/shp-cli/src/index.ts\"",
      "cmp fixtures/skills/unix-system-visualiser/connected/.research/atlas-a.html fixtures/skills/unix-system-visualiser/connected/.research/atlas-b.html"
    ]
  },
  {
    "id": "visualiser-unignored-output",
    "skill": "unix-system-visualiser",
    "task": "Attempt the default generation in a repository that explicitly does not ignore .research, and verify that the skill blocks before writing the HTML artifact.",
    "fixture": "fixtures/skills/unix-system-visualiser/unignored-output",
    "required_commands": [
      "bun plugins/shapelang/skills/unix-system-visualiser/scripts/generate.mjs --repo fixtures/skills/unix-system-visualiser/unignored-output --shape-command \"bun ../../../../packages/shp-cli/src/index.ts\""
    ]
  }
]
