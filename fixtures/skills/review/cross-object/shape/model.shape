module skill_review_cross_object

component Gateway {
  fn handleRequest
    source ts("fixtures/skills/review/cross-object/candidate/gateway.ts#handleRequest")
    effects complete {
    }
}

component PolicyService {
  fn getPolicy
    source ts("fixtures/skills/review/cross-object/candidate/policy.ts#getPolicy")
    effects complete {
    }
}

relation GatewayCallsPolicy {
  kind calls
  connects Gateway -> PolicyService
}
