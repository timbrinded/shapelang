module skill_review_stale

component Gateway {
  fn loadConfig
    source ts("fixtures/skills/review/stale-model/candidate/config.ts#loadConfig")
    effects complete {
    }
}

component RemoteConfig {
}

relation GatewayCallsRemoteConfig {
  kind calls
  connects Gateway -> RemoteConfig
}
